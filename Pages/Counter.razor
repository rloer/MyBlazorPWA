@page "/counter"
@inject IJSRuntime Jsr 

<h1>Contador</h1>
<p>Current count: @currentCount</p>
<button class="btn btn-primary" @onclick="IncrementCount">Da Click Aquí</button>
@* <div id="reactProgressBar"></div> *@


@code {
    private int currentCount = 0;

    [Parameter]
    public int IncrementoCantidad { get; set; } = 1;

    
    @* private async void IncrementCount() *@
    private void IncrementCount()
    {
        //currentCount++;
        currentCount = currentCount + IncrementoCantidad;
        @* await Jsr.InvokeVoidAsync("RenderProgressBar", currentCount); *@

        

    }
}
